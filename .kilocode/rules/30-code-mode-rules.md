# Aturan Code Mode
- Implementasi nyata sesuai micro-plan; doc comment + ≥1 test per
  public item; daftarkan ke catalog (legacy=false, quality=real).
- Triage domain milik WP: fake=0, ≥12 real/domain, ID TC-U-* benar.
- Jalankan gate sebelum handoff ke orchestrator.
- Update STUB_INVENTORY (tutup baris), IMPLEMENTATION_SUMMARY
  (traceability), DECISIONS bila perlu.
- Wiring-only di luar scope harus disebut eksplisit di handoff note.
